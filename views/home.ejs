<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Message Board | Home</title>
</head>
<body>
    <%- include("header") -%>
    
    <h1>Messages</h1>
    <a href="/new-message">New message</a>
    <ul>
        <% messages.forEach( message =>{ %>
            <li>
                <h2><a href="messages/<%=message.id%>"><%=message.title%></a></h2>
                <p><%=message.content.substr(0, 200)%></p>
                <p><%=message.date.toString().substr(0, 15)%></p>
                <p><%=message.replies.length%> replies</p>
            </li>
        <% }); %>
    </ul>
</body>
</html>